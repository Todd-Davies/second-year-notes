\documentclass[dvipsnames]{minimal}

\usepackage{tikz}
\usepackage[pdftex,active,tightpage]{preview}
\PreviewEnvironment{tikzpicture}

\usetikzlibrary{arrows, automata, positioning}

\author{Todd Davies}
\date{\today}

\begin{document}

\begin{center}
\begin{center}\begin{tikzpicture}[transform shape,>=stealth',shorten >=1pt,auto,node distance=2cm]
  \node[state]                        (STR)                    {\texttt{STR}};

  \node[state]                        (MUL) [above of=STR] {\texttt{MUL}};

  \node[state]                        (ADD) [above left=1cm and 2cm of MUL]     {\texttt{ADD}};
  \node[state]                        (SUB) [above right=1cm and 2cm of MUL] {\texttt{SUB}};

  \node[]                             (R2) [above left of=ADD]  {R2};
  \node[]                             (R3) [above right of=ADD] {R3};
  \node[]                             (R4) [above left of=SUB]  {R4};
  \node[]                             (R5) [above right of=SUB] {R5};

  \path[->]          (R2) edge                   node        {} (ADD);
  \path[->]          (R3) edge                   node        {} (ADD);

  \path[->]          (R4) edge                   node        {} (SUB);
  \path[->]          (R5) edge                   node        {} (SUB);

  \path[->]          (ADD) edge                   node        {} (MUL);
  \path[->]          (SUB) edge                   node        {} (MUL);

  \path[->]          (MUL) edge                   node        {} (STR);
\end{tikzpicture}\end{center}
\end{center}

\end{document}
